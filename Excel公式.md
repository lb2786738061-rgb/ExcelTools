# Excel 全场景求和公式/函数大全（按使用频率&场景分类，覆盖99%求和需求）
## 一、基础核心求和（最常用，入门必学）
### 1. SUM 函数（最基础的无条件求和）
**核心功能**：对指定的单元格、单元格区域、常量进行求和，是所有求和场景的基础
**语法**：
```excel
SUM(数值1, [数值2], [数值3], ...)
```
- 参数：可以是单元格区域、单个单元格、数字、数组，最多支持255个参数
**适用场景**：
- 连续单元格区域求和
- 多个分散单元格/区域合并求和
- 固定数值求和
**实用示例**：
```excel
=SUM(A1:A10)  // 对A1到A10的连续单元格求和
=SUM(A1:A10,C1:C10)  // 同时对A列1-10行、C列1-10行两个区域求和
=SUM(10,20,30)  // 对固定数字求和，结果为60
=SUM(A1:A10,100)  // 区域求和后再加固定值100
```

---

## 二、条件求和（按规则筛选后求和，工作中80%的求和需求都用这类）
### 1. SUMIF 函数（单条件求和）
**核心功能**：对满足**1个条件**的单元格区域求和
**语法**：
```excel
SUMIF(条件区域, 条件, [求和区域])
```
- 条件区域：用来判断是否满足条件的单元格区域
- 条件：求和的规则（文本、数字、表达式、单元格引用）
- 求和区域：可选，需要实际求和的单元格区域；省略时默认和条件区域一致
**适用场景**：
- 按部门、按品类、按状态等单一维度筛选求和
- 模糊匹配求和（比如包含某个关键词的内容求和）
**实用示例**：
```excel
=SUMIF(B:B,"销售部",C:C)  // 求B列中为“销售部”的所有行，C列的业绩总和
=SUMIF(A:A,">100",A:A)  // 求A列中大于100的数值总和
=SUMIF(A:A,"*苹果*",B:B)  // 求A列中包含“苹果”关键词的行，B列的销量总和
=SUMIF(C:C,"是",D:D)  // 求C列标记为“是”的行，D列的金额总和
```

### 2. SUMIFS 函数（多条件求和，Excel 2007及以上版本支持）
**核心功能**：对**同时满足多个条件**的单元格区域求和，是SUMIF的多条件增强版
**语法**：
```excel
SUMIFS(求和区域, 条件区域1, 条件1, 条件区域2, 条件2, ...)
```
- 求和区域：需要实际求和的单元格区域（必须放在最前面，和SUMIF顺序相反，核心易错点）
- 条件区域1/条件1：第一组条件的区域和规则
- 支持最多127组条件，所有条件必须同时满足才会被计入求和
**适用场景**：
- 多维度筛选求和（比如按部门+月份、按品类+区域+状态）
- 区间范围求和（比如大于100且小于500的数值求和）
**实用示例**：
```excel
=SUMIFS(D:D,B:B,"销售部",C:C,"1月")  // 求销售部1月的业绩总和（2个条件）
=SUMIFS(A:A,A:A,">100",A:A,"<500")  // 求A列中大于100且小于500的数值总和
=SUMIFS(E:E,B:B,"华东区",C:C,"电子产品",D:D,">1000")  // 3个条件：华东区+电子产品+单价>1000，求总销售额
```

---

## 三、数组/乘积求和（复杂计算、权重、多条件兼容场景）
### 1. SUMPRODUCT 函数（乘积求和，全能型求和函数）
**核心功能**：将多个数组的对应元素相乘，再对乘积求和；同时可替代SUMIF/SUMIFS实现多条件求和，兼容性覆盖所有Excel版本
**语法**：
```excel
SUMPRODUCT(数组1, [数组2], [数组3], ...)
```
- 数组1/2/3：需要相乘后求和的单元格区域，必须大小维度一致
**适用场景**：
- 加权求和（比如数量×单价求总金额、学分×绩点求总绩点）
- 旧版Excel的多条件求和（替代SUMIFS，兼容性更好）
- 多条件计数+求和合并计算
**实用示例**：
```excel
// 基础用法：加权求和，求总销售额（数量列×单价列）
=SUMPRODUCT(B1:B10,C1:C10)  // 等价于B1*C1+B2*C2+...+B10*C10

// 多条件求和用法（替代SUMIFS）：求销售部1月的业绩总和
=SUMPRODUCT((B:B="销售部")*(C:C="1月")*D:D)

// 区间求和：求A列大于100且小于500的数值总和
=SUMPRODUCT((A:A>100)*(A:A<500)*A:A)
```

### 2. 数组公式求和（旧版Excel数组计算，Excel 365已被动态数组替代）
**核心功能**：对数组进行批量计算后求和，旧版Excel需要按`Ctrl+Shift+Enter`确认数组公式
**语法示例**：
```excel
=SUM(IF((B:B="销售部")*(C:C="1月"),D:D,0))  // 旧版Excel多条件求和数组公式
=SUM(ROW(A1:A10))  // 对1-10的行号求和，结果为55
```

---

## 四、筛选/可见单元格求和（隐藏行/筛选后专用，避坑必学）
### 1. SUBTOTAL 函数（忽略隐藏行/筛选后求和）
**核心功能**：对可见单元格求和，自动忽略手动隐藏的行、筛选掉的行；**SUM函数会包含隐藏行的数值，这是核心区别**
**语法**：
```excel
SUBTOTAL(功能代码, 求和区域)
```
- 功能代码：求和用`9`（包含隐藏值）、`109`（忽略隐藏值，推荐）
- 求和区域：需要求和的单元格区域
**适用场景**：
- 筛选数据后，只对筛选结果求和
- 手动隐藏部分行后，只对可见行求和
- 制作动态报表，隐藏行不影响求和结果
**实用示例**：
```excel
=SUBTOTAL(109,C:C)  // 对C列可见单元格求和，忽略隐藏行、筛选掉的行
=SUBTOTAL(9,A1:A10)  // 对A1:A10求和，包含隐藏行（和SUM效果一致）
```
> 核心注意：SUBTOTAL**只忽略行隐藏，不忽略列隐藏**；且函数本身不会计算其他SUBTOTAL所在的单元格，避免重复计算。

### 2. AGGREGATE 函数（SUBTOTAL增强版，Excel 2010及以上支持）
**核心功能**：SUBTOTAL的升级版，支持忽略隐藏行/列、错误值，功能更全面
**语法**：
```excel
AGGREGATE(功能代码, 忽略规则, 求和区域)
```
- 求和功能代码：`9`代表求和
- 忽略规则：`3`代表忽略隐藏行、错误值，`7`代表忽略隐藏行/列、错误值
**适用场景**：
- 筛选后求和，同时忽略区域内的错误值（#DIV/0!、#N/A等）
- 列隐藏后也能正确求和
**实用示例**：
```excel
=AGGREGATE(9,3,A1:A10)  // 对A1:A10求和，忽略隐藏行和错误值
=AGGREGATE(9,7,A1:A10)  // 对A1:A10求和，忽略隐藏行、列和错误值
```

---

## 五、Excel 365/2021 动态数组求和（新版专属，更灵活）
### 1. FILTER+SUM 动态条件求和
**核心功能**：先按条件筛选出符合要求的区域，再对筛选结果求和，支持动态更新
**实用示例**：
```excel
=SUM(FILTER(D:D,(B:B="销售部")*(C:C="1月")))  // 动态筛选销售部1月的业绩，再求和
=SUM(FILTER(A:A,A:A>100))  // 动态筛选A列大于100的数值，再求和
```

### 2. BYROW/BYCOL 批量按行/列求和
**核心功能**：批量对每一行/每一列求和，自动溢出结果，无需下拉填充
**实用示例**：
```excel
=BYROW(A1:C10,LAMBDA(x,SUM(x)))  // 对A1:C10的每一行分别求和，自动溢出每行的结果
=BYCOL(A1:C10,LAMBDA(x,SUM(x)))  // 对A1:C10的每一列分别求和，自动溢出每列的结果
```

---

## 六、特殊场景求和公式（高频实用场景）
### 1. 累计求和（逐行累加）
**核心公式**：
```excel
=SUM($A$1:A1)  // 下拉填充，实现A1到当前行的累计求和
```
- 原理：$A$1是绝对引用，固定起始点；A1是相对引用，下拉时自动变为A2、A3...

### 2. 隔行/隔列求和
**核心公式**：
```excel
=SUMIF(MOD(ROW(A1:A10),2),1,A1:A10)  // 对A1:A10的奇数行求和（数组公式，旧版需Ctrl+Shift+Enter）
=SUMIF(MOD(COLUMN(A1:J1),2),0,A1:J1)  // 对A1:J1的偶数列求和
```

### 3. 跨表求和
**核心公式**：
```excel
=SUM(Sheet1!A1:A10,Sheet2!A1:A10)  // 同时对Sheet1和Sheet2的A1:A10求和
=SUM('1月:3月'!A1:A10)  // 对1月、2月、3月三个工作表的A1:A10批量求和
```

### 4. 忽略错误值求和
**核心公式**：
```excel
=SUMIF(A1:A10,"<>#N/A")  // 忽略#N/A错误值求和
=AGGREGATE(9,6,A1:A10)  // 忽略所有错误值求和（Excel 2010+）
=SUM(IFERROR(A1:A10,0))  // 旧版Excel，把错误值转为0再求和（数组公式）
```

---

## 七、常用求和场景速查表
| 求和场景 | 首选函数 | 备选函数 |
|----------|----------|----------|
| 简单连续区域求和 | SUM | - |
| 单条件筛选求和 | SUMIF | SUMPRODUCT |
| 多条件同时满足求和 | SUMIFS | SUMPRODUCT |
| 筛选后/可见单元格求和 | SUBTOTAL(109,...) | AGGREGATE |
| 加权求和/乘积求和 | SUMPRODUCT | - |
| 忽略错误值求和 | AGGREGATE | SUMIF/IFERROR+SUM |
| 新版Excel动态条件求和 | SUM+FILTER | - |
| 累计求和 | SUM+绝对引用 | - |

---

## 核心避坑要点
1. **SUMIF和SUMIFS参数顺序相反**：SUMIF是「条件区域→条件→求和区域」，SUMIFS是「求和区域→条件区域1→条件1」，写反会直接出错。
2. **筛选后求和不要用SUM**：SUM会包含隐藏行/筛选掉的行，必须用SUBTOTAL(109,...)。
3. **条件里的文本要加英文双引号**：比如"销售部"，不能用中文引号，否则会报错。
4. **多条件求和的条件区域维度必须一致**：SUMIFS/SUMPRODUCT的所有条件区域、求和区域，必须是相同的行数/列数，否则会出错。